# StarMind Manager v2.0 完整测试文档

> 版本：v2.0 | 日期：2026-06-06 | 测试环境：Windows 11 Pro
> 项目路径：`C:\Users\84787\Desktop\GetGithub`
> 本文档覆盖全部 24 项功能的系统性测试用例

---

## 1. 测试环境准备

### 1.1 硬件要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Windows 10 64-bit | Windows 11 Pro |
| CPU | 双核 2GHz | 四核 3GHz+ |
| 内存 | 4GB | 8GB+ |
| 磁盘空间 | 500MB | 1GB+ |
| 网络 | 可访问 GitHub API | 稳定宽带 |

### 1.2 软件依赖安装

| 编号 | 操作 | 预期结果 |
|------|------|---------|
| E-1.1 | 安装 Python 3.10+，终端执行 `python --version` | 显示 Python 3.10.x 或更高版本 |
| E-1.2 | 执行 `pip install customtkinter openai jinja2 requests` | 所有包安装成功无报错 |
| E-1.3 | 确认 customtkinter 已安装：`pip show customtkinter` | 显示版本号和路径 |
| E-1.4 | 确认 openai SDK 已安装：`pip show openai` | 显示版本号和路径 |
| E-1.5 | 确认 jinja2 已安装：`pip show jinja2` | 显示版本号和路径 |
| E-1.6 | 确认 requests 已安装：`pip show requests` | 显示版本号和路径 |
| E-1.7 | 准备有效的 GitHub Personal Access Token（scope: repo, read:user） | Token 可正常调用 API |
| E-1.8 | 准备可用的 OpenAI 兼容 LLM 接口（DeepSeek/Ollama/Qwen 等） | test_connection 返回成功 |

### 1.3 测试数据准备

| 编号 | 操作 | 预期结果 |
|------|------|---------|
| E-2.1 | 备份当前 starmind.db（若存在），复制为 starmind.db.bak | 备份文件创建成功 |
| E-2.2 | 备份当前 config.json，复制为 config.json.bak | 备份文件创建成功 |
| E-2.3 | 删除 starmind.db 以获得干净测试环境 | 文件删除成功 |
| E-2.4 | 确认 templates/index.html 和 templates/compact.html 存在 | 两个模板文件均存在 |
| E-2.5 | 运行测试数据准备脚本，插入 15 条测试数据 | 显示已插入 15 条数据 |
| E-2.6 | 运行 db.get_repo_count() 验证可见数 | 返回 14（隐藏项目不计入） |
| E-2.7 | 运行 db.get_repo_count(include_hidden=True) 验证总数 | 返回 15 |

### 1.4 测试数据集

以下 15 条测试数据贯穿全文全部测试用例（14 条可见 + 1 条隐藏）：

| ID | 名称 | Star | 分类 | 语言 | 标签 | 状态 |
|----|------|------|------|------|------|------|
| 100001 | test-user/ai-project | 15000 | AI与大模型 | Python | LLM, RAG, Python | 正常 |
| 100002 | test-user/web-framework | 8000 | 后端开发 | Go | Web, Go, Framework | 正常 |
| 100003 | test-user/react-ui | 5000 | 前端开发 | TypeScript | React, UI, TypeScript | 正常 |
| 100004 | test-user/db-tool | 300 | 数据库与存储 | Rust | Database, Migration, SQL | 正常 |
| 100005 | test-user/devops-pipeline | 2000 | 运维/DevOps | Shell | CI/CD, Docker, Kubernetes | 正常 |
| 100006 | other-user/mobile-app | 4500 | 移动端开发 | Dart | Flutter, Dart, Firebase | 正常 |
| 100007 | test-user/security-scanner | 600 | 测试与安全 | Python | Security, SAST, Python | 正常 |
| 100008 | test-user/note-app | 120 | 桌面系统应用 | JavaScript | Electron, Markdown, Note | 正常 |
| 100009 | test-user/web-scraper | 900 | 爬虫与数据提取 | Python | Scrapy, Crawler, Python | 正常 |
| 100010 | test-user/media-player | 2500 | 影音媒体处理 | C++ | FFmpeg, MediaPlayer, C++ | 正常 |
| 100011 | test-user/game-engine | 7000 | 独立游戏与开发引擎 | C | GameEngine, Lua, ECS | 正常 |
| 100012 | test-user/defi-protocol | 1800 | 区块链/Web3 | Solidity | DeFi, Solidity, Ethereum | 正常 |
| 100013 | test-user/python-tutorial | 50000 | 学习教程与资料 | Python | Python, Tutorial, Beginner | 正常 |
| 100014 | test-user/hidden-project | 50 | 其他 | Python | Test | 隐藏 |
| 100015 | other-user/another-ai | 12000 | AI与大模型 | Python | LLM, Multimodal, Python | 正常 |

预创建集合：**AI收藏**（含 100001, 100015）、**学习资源**（含 100013）


---

## 2. 数据库模块测试

> 测试对象：`db.py`（525 行，28 个函数）
> 测试策略：覆盖全部函数，验证 CRUD 正确性、外键约束、软删除隔离、批量操作原子性

### 2.1 Schema 初始化测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| DB-001 | Schema | init_db 创建主表 | 1. 删除 starmind.db 2. 调用 init_db() 3. 查询 sqlite_master | starred_repos 表存在，包含 id/name/stars/summary/category/tags/language/url/description/processed_date/owner_username/starred_at 共 12 列 | | |
| DB-002 | Schema | init_db 创建集合表 | 同上，检查 collections 和 collection_items 表 | collections 表含 id/name/created_at 列；collection_items 表含 collection_id/repo_id 复合主键 | | |
| DB-003 | Schema | init_db 幂等性 | 连续调用 init_db() 两次 | 第二次调用无报错，表结构不变 | | |
| DB-004 | Schema | 自动迁移添加隐藏列 | 1. 创建不含 hidden 列的旧表 2. 调用 init_db() 3. 查询表结构 | starred_repos 表新增 hidden INTEGER DEFAULT 0 列 | | |
| DB-005 | Schema | 自动迁移添加 notes 列 | 同上方式检查 notes 列 | starred_repos 表新增 notes TEXT 列 | | |
| DB-006 | Schema | 自动迁移添加 favorite 列 | 同上方式检查 favorite 列 | starred_repos 表新增 favorite INTEGER DEFAULT 0 列 | | |
| DB-007 | Schema | collection_items 外键约束 | 1. 调用 init_db() 2. 尝试插入不存在的 repo_id | 抛出 sqlite3.IntegrityError 外键约束异常 | | |

### 2.2 基础 CRUD 测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| DB-008 | CRUD | get_connection 连接创建 | 调用 get_connection() | 返回 sqlite3.Connection 对象，row_factory 为 sqlite3.Row | | |
| DB-009 | CRUD | upsert_repo 插入新记录 | 调用 upsert_repo(test_repos[0], "test-user") | get_repo_by_id(100001) 返回正确数据，tags 解析为 ["LLM","RAG","Python"] | | |
| DB-010 | CRUD | upsert_repo 更新已有记录 | 1. upsert_repo 插入 id=100001 2. 修改 stars 为 20000 再次 upsert | get_repo_by_id(100001).stars 等于 20000，不产生重复记录 | | |
| DB-011 | CRUD | upsert_repo tags 序列化 | upsert_repo 传入 tags=["A","B","C"] | 数据库中 tags 字段为 JSON 字符串 '["A","B","C"]'，get_repo_by_id 返回列表 | | |
| DB-012 | CRUD | get_all_repos 默认排除隐藏 | 调用 get_all_repos() | 返回 14 条记录，不含 id=100014（hidden=1） | | |
| DB-013 | CRUD | get_all_repos 包含隐藏 | 调用 get_all_repos(include_hidden=True) | 返回 15 条记录，包含 id=100014 | | |
| DB-014 | CRUD | get_all_repos 排序 | 调用 get_all_repos() 检查顺序 | 按 stars 降序排列，第一条为 id=100013（50000 stars） | | |
| DB-015 | CRUD | get_repo_by_id 存在 | 调用 get_repo_by_id(100001) | 返回 dict，name 为 "test-user/ai-project"，category 为 "AI与大模型" | | |
| DB-016 | CRUD | get_repo_by_id 不存在 | 调用 get_repo_by_id(999999) | 返回 None | | |
| DB-017 | CRUD | get_repo_count 默认 | 调用 get_repo_count() | 返回 14 | | |
| DB-018 | CRUD | get_repo_count 含隐藏 | 调用 get_repo_count(include_hidden=True) | 返回 15 | | |
| DB-019 | CRUD | get_existing_ids | 调用 get_existing_ids("test-user") | 返回集合包含 100001~100014 共 14 个 ID | | |
| DB-020 | CRUD | get_existing_ids 空用户 | 调用 get_existing_ids("") | 返回 owner_username 为空或 NULL 的记录 ID 集合 | | |
| DB-021 | CRUD | _rows_to_repos 解析 | 对含 tags 为 JSON 字符串的 Row 调用 _rows_to_repos | 返回的 dict 中 tags 为 Python 列表 | | |
| DB-022 | CRUD | _rows_to_repos 容错 | 对 tags 为非法 JSON 的 Row 调用 _rows_to_repos | tags 字段返回空列表 []，不抛异常 | | |

### 2.3 分页查询测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| DB-023 | 分页 | get_repos_paged 基本分页 | 调用 get_repos_paged(page=1, page_size=5) | 返回 (repos_list, total_count)，repos_list 长度 5，total_count 为 14 | | |
| DB-024 | 分页 | get_repos_paged 第二页 | 调用 get_repos_paged(page=2, page_size=5) | 返回 5 条记录，与第一页不重叠 | | |
| DB-025 | 分页 | get_repos_paged 最后一页 | 调用 get_repos_paged(page=3, page_size=5) | 返回 4 条记录（14-10=4） | | |
| DB-026 | 分页 | get_repos_paged 分类筛选 | 调用 get_repos_paged(category="AI与大模型") | 仅返回 category 为 "AI与大模型" 的项目（100001, 100015） | | |
| DB-027 | 分页 | get_repos_paged 搜索 | 调用 get_repos_paged(search="python") | 返回 name/summary/description 中包含 "python" 的项目 | | |
| DB-028 | 分页 | get_repos_paged 含隐藏 | 调用 get_repos_paged(include_hidden=True) | total_count 为 15，结果含 id=100014 | | |
| DB-029 | 分页 | get_repos_paged 空结果 | 调用 get_repos_paged(search="不存在的关键词xyz") | 返回 ([], 0) | | |

### 2.4 软删除/硬删除/收藏测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| DB-030 | 删除 | toggle_hidden 隐藏 | 调用 toggle_hidden(100001, True) | get_repo_by_id(100001) 的 hidden 字段为 1 | | |
| DB-031 | 删除 | toggle_hidden 恢复 | 调用 toggle_hidden(100001, False) | get_repo_by_id(100001) 的 hidden 字段为 0 | | |
| DB-032 | 删除 | hard_delete_repo | 1. 调用 hard_delete_repo(100004) 2. get_repo_by_id(100004) | 返回 None，记录从数据库永久删除 | | |
| DB-033 | 删除 | hard_delete_repo 清理集合关联 | 1. 将 100001 加入 "学习资源" 集合 2. hard_delete_repo(100001) 3. get_collection_repos("学习资源") | 不再包含 id=100001 的项目 | | |
| DB-034 | 收藏 | toggle_favorite 切换开 | 调用 toggle_favorite(100002) | 返回 True，数据库中 favorite=1 | | |
| DB-035 | 收藏 | toggle_favorite 切换关 | 再次调用 toggle_favorite(100002) | 返回 False，数据库中 favorite=0 | | |
| DB-036 | 收藏 | toggle_favorite 不存在的 ID | 调用 toggle_favorite(999999) | 返回 False，不抛异常 | | |

### 2.5 手动编辑测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| DB-037 | 编辑 | update_repo_metadata 修改摘要 | 调用 update_repo_metadata(100003, summary="新摘要") | get_repo_by_id(100003).summary 等于 "新摘要" | | |
| DB-038 | 编辑 | update_repo_metadata 修改分类 | 调用 update_repo_metadata(100003, category="效率辅助工具") | category 字段变为 "效率辅助工具" | | |
| DB-039 | 编辑 | update_repo_metadata 修改标签 | 调用 update_repo_metadata(100003, tags=["Vue","Svelte"]) | tags 字段变为 ["Vue","Svelte"] | | |
| DB-040 | 编辑 | update_repo_metadata 修改备注 | 调用 update_repo_metadata(100003, notes="重要项目") | notes 字段变为 "重要项目" | | |
| DB-041 | 编辑 | update_repo_metadata 多字段同时修改 | 调用 update_repo_metadata(100003, summary="S2", category="C2", tags=["T1"], notes="N1") | 四个字段全部更新正确 | | |
| DB-042 | 编辑 | update_repo_metadata 无参数 | 调用 update_repo_metadata(100003) | 不修改任何字段，不抛异常 | | |

### 2.6 批量操作测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| DB-043 | 批量 | batch_toggle_hidden 批量隐藏 | 调用 batch_toggle_hidden([100005, 100006], True) | 两条记录 hidden=1，get_all_repos() 不包含它们 | | |
| DB-044 | 批量 | batch_toggle_hidden 批量恢复 | 调用 batch_toggle_hidden([100005, 100006], False) | 两条记录 hidden=0，get_all_repos() 包含它们 | | |
| DB-045 | 批量 | batch_toggle_hidden 空列表 | 调用 batch_toggle_hidden([], True) | 不抛异常，无副作用 | | |
| DB-046 | 批量 | batch_delete 批量硬删除 | 调用 batch_delete([100008, 100009]) | 两条记录从数据库永久删除，get_repo_count() 减少 2 | | |
| DB-047 | 批量 | batch_delete 清理集合关联 | 1. 将 100010 加入 "AI收藏" 2. batch_delete([100010]) | collection_items 中不再有 repo_id=100010 的记录 | | |
| DB-048 | 批量 | batch_delete 空列表 | 调用 batch_delete([]) | 不抛异常，无副作用 | | |
| DB-049 | 批量 | batch_update_category | 调用 batch_update_category([100011, 100012], "其他") | 两条记录 category 变为 "其他" | | |
| DB-050 | 批量 | batch_update_category 空列表 | 调用 batch_update_category([], "其他") | 不抛异常 | | |

### 2.7 集合管理测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| DB-051 | 集合 | create_collection 新建 | 调用 create_collection("测试集合") | 返回新集合 ID（正整数），get_collections() 中出现 "测试集合" | | |
| DB-052 | 集合 | create_collection 重复名称 | 两次调用 create_collection("测试集合") | 第二次返回已有集合的 ID，不产生重复记录 | | |
| DB-053 | 集合 | delete_collection | 1. 创建 "临时集合" 2. 添加项目 3. delete_collection("临时集合") | get_collections() 中不再有 "临时集合"，关联的 collection_items 也被删除 | | |
| DB-054 | 集合 | delete_collection 不存在 | 调用 delete_collection("不存在的集合") | 不抛异常 | | |
| DB-055 | 集合 | get_collections 列表 | 调用 get_collections() | 返回 list，每个元素含 id/name/created_at/repo_count | | |
| DB-056 | 集合 | get_collections 数量统计 | 检查 "AI收藏" 的 repo_count | 等于 2（含 100001 和 100015） | | |
| DB-057 | 集合 | add_to_collection | 调用 add_to_collection("AI收藏", 100002) | get_collection_repos("AI收藏") 包含 id=100002 的项目 | | |
| DB-058 | 集合 | add_to_collection 重复添加 | 两次调用 add_to_collection("AI收藏", 100001) | 不抛异常，集合中不产生重复条目 | | |
| DB-059 | 集合 | add_to_collection 不存在的集合 | 调用 add_to_collection("ghost", 100001) | 不抛异常，不插入数据 | | |
| DB-060 | 集合 | remove_from_collection | 调用 remove_from_collection("AI收藏", 100001) | get_collection_repos("AI收藏") 不再包含 id=100001 | | |
| DB-061 | 集合 | remove_from_collection 不存在的集合 | 调用 remove_from_collection("ghost", 100001) | 不抛异常 | | |
| DB-062 | 集合 | batch_add_to_collection | 调用 batch_add_to_collection("学习资源", [100001, 100002]) | get_collection_repos("学习资源") 新增这两个项目 | | |
| DB-063 | 集合 | batch_add_to_collection 空列表 | 调用 batch_add_to_collection("学习资源", []) | 不抛异常 | | |
| DB-064 | 集合 | get_collection_repos | 调用 get_collection_repos("AI收藏") | 返回列表，包含 "AI收藏" 中所有项目，按 stars 降序 | | |
| DB-065 | 集合 | get_repo_collections | 调用 get_repo_collections(100001) | 返回包含 "AI收藏" 的列表 | | |
| DB-066 | 集合 | get_repo_collections 无集合 | 调用 get_repo_collections(100004)（未加入任何集合） | 返回空列表 [] | | |

### 2.8 数据统计查询测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| DB-067 | 查询 | get_all_categories | 调用 get_all_categories() | 返回去重排序的分类列表，包含 "AI与大模型","后端开发" 等，不含空值 | | |
| DB-068 | 查询 | get_all_categories 排除隐藏 | 检查结果中是否包含隐藏项目的分类 | 如果隐藏项目是唯一属于某分类的，则该分类不出现在结果中 | | |
| DB-069 | 查询 | get_usernames | 调用 get_usernames() | 返回 ["other-user", "test-user"]（排序后） | | |
| DB-070 | 查询 | get_repos_by_username | 调用 get_repos_by_username("test-user") | 返回 14 条记录（test-user 名下的全部项目，含隐藏） | | |
| DB-071 | 查询 | get_repos_by_username 不存在 | 调用 get_repos_by_username("ghost") | 返回空列表 [] | | |
| DB-072 | 查询 | search_repos 关键词搜索 | 调用 search_repos(query="Python") | 返回 name/summary/description/tags 中含 "Python" 的项目 | | |
| DB-073 | 查询 | search_repos 分类过滤 | 调用 search_repos(query="", category="AI与大模型") | 仅返回 category="AI与大模型" 的可见项目 | | |
| DB-074 | 查询 | search_repos 语言过滤 | 调用 search_repos(query="", language="Python") | 仅返回 language="Python" 的可见项目 | | |
| DB-075 | 查询 | search_repos Star 范围 | 调用 search_repos(query="", min_stars=5000, max_stars=20000) | 仅返回 5000<=stars<=20000 的项目 | | |
| DB-076 | 查询 | search_repos 复合条件 | 调用 search_repos(query="ai", category="AI与大模型", language="Python") | 返回同时满足三个条件的项目 | | |
| DB-077 | 查询 | search_repos 含隐藏 | 调用 search_repos(query="hidden", include_hidden=True) | 结果包含 hidden=1 的项目 | | |
| DB-078 | 查询 | search_repos 无结果 | 调用 search_repos(query="不存在的xyz") | 返回空列表 [] | | |


---

## 3. GitHub API 模块测试

> 测试对象：`github_api.py`（193 行，6 个公开函数 + 1 个内部函数）
> 测试策略：使用真实 GitHub Token 调用 API，验证返回数据格式和分页逻辑

### 3.1 连接与认证测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| API-001 | 认证 | _headers 构造 | 调用 _headers("ghp_test") | 返回 dict 包含 Authorization: "token ghp_test" 和 Accept 头 | | |
| API-002 | 认证 | check_rate_limit 有效 Token | 使用有效 Token 调用 check_rate_limit(token) | 返回 dict 含 limit(整数)、remaining(整数)、reset(整数) 三个键 | | |
| API-003 | 认证 | check_rate_limit 无效 Token | 使用无效 Token "ghp_invalid" 调用 check_rate_limit | 抛出 requests.exceptions.HTTPError（401 Unauthorized） | | |
| API-004 | 认证 | check_rate_limit remaining 非负 | 调用 check_rate_limit(valid_token) | remaining >= 0 且 remaining <= limit | | |

### 3.2 Star 列表获取测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| API-005 | 同步 | fetch_starred_repos 自己的 Star | 调用 fetch_starred_repos(token) 不传 username | 返回 list，每项含 id/name/stars/language/url/description/topics/starred_at 字段 | | |
| API-006 | 同步 | fetch_starred_repos 指定用户 | 调用 fetch_starred_repos(token, "torvalds") | 返回 torvalds 用户公开 Star 列表，结果非空 | | |
| API-007 | 同步 | fetch_starred_repos 分页验证 | 获取结果后检查 starred_at 字段 | 每个条目 starred_at 为 ISO 8601 格式字符串（如 "2026-01-15T10:00:00Z"） | | |
| API-008 | 同步 | fetch_starred_repos 回调函数 | 传入 callback=lambda p,r: print(p,len(r)) | 每页获取后回调被调用，参数 page 从 1 递增 | | |
| API-009 | 同步 | fetch_starred_repos 不存在的用户 | 调用 fetch_starred_repos(token, "this_user_definitely_not_exist_abc123xyz") | 抛出 HTTPError（404）或返回空列表 | | |

### 3.3 项目内容获取测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| API-010 | 内容 | fetch_readme 有 README | 调用 fetch_readme(token, "torvalds/linux") | 返回非空字符串，内容为 Linux 仓库 README 的纯文本 | | |
| API-011 | 内容 | fetch_readme 无 README | 调用 fetch_readme(token, 创建一个无 README 的空仓库名) | 返回空字符串 "" | | |
| API-012 | 内容 | fetch_repo_tree 有效仓库 | 调用 fetch_repo_tree(token, "torvalds/linux") | 返回格式化文本，包含"项目 torvalds/linux 的文件结构："前缀，含文件和目录列表 | | |
| API-013 | 内容 | fetch_repo_tree 无效仓库 | 调用 fetch_repo_tree(token, "ghost/ghost") | 返回空字符串 "" | | |
| API-014 | 内容 | fetch_repo_info 有效仓库 | 调用 fetch_repo_info(token, "torvalds/linux") | 返回 dict 含 description/topics/language/homepage 四个键 | | |


---

## 4. LLM 模块测试

> 测试对象：`llm.py`（168 行，6 个函数）
> 测试策略：测试 JSON 解析容错、多级降级策略、重试逻辑、连通性检测

### 4.1 JSON 解析测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| LLM-001 | 解析 | _parse_llm_json 标准 JSON | 传入 '{"summary":"test","category":"AI","tags":["a","b"]}' | 返回 dict，summary="test", tags=["a","b"] | | |
| LLM-002 | 解析 | _parse_llm_json Markdown 包裹 | 传入 '```json\n{"summary":"s","category":"c","tags":[]}\n```' | 正确提取 JSON，返回 dict | | |
| LLM-003 | 解析 | _parse_llm_json 含前缀文本 | 传入 '这是分析结果：\n{"summary":"s","category":"c","tags":["t"]}' | 正则提取大括号内容，返回 dict | | |
| LLM-004 | 解析 | _parse_llm_json tags 为字符串 | 传入 '{"summary":"s","category":"c","tags":"single_tag"}' | 返回 dict，tags 被转换为 ["single_tag"] | | |
| LLM-005 | 解析 | _parse_llm_json tags 为 null | 传入 '{"summary":"s","category":"c","tags":null}' | 返回 dict，tags 为 [] | | |
| LLM-006 | 解析 | _parse_llm_json 缺少必填字段 | 传入 '{"category":"c"}' （无 summary） | 返回 None | | |
| LLM-007 | 解析 | _parse_llm_json 非法 JSON | 传入 '这不是JSON' | 返回 None | | |

### 4.2 多级降级策略测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| LLM-008 | 降级 | 策略1：使用 Readme | 调用 summarize_repo(base_url, api_key, model, readme_text="这是一个 Python 项目...") | LLM 被调用时 user_content 包含 Readme 内容前 1500 字符 | | |
| LLM-009 | 降级 | 策略2：使用 Description | 调用 summarize_repo(base_url, api_key, model, readme_text="", description="Fast HTTP framework") | LLM 被调用时 user_content 包含 Description | | |
| LLM-010 | 降级 | 策略3：使用文件结构 | 调用 summarize_repo(base_url, api_key, model, readme_text="", description="", repo_tree="文件结构...") | LLM 被调用时 user_content 包含 repo_tree | | |
| LLM-011 | 降级 | 所有内容为空 | 调用 summarize_repo(base_url, api_key, model) 所有内容参数为空 | 返回 None | | |

### 4.3 连通性与重试测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| LLM-012 | 连通 | test_connection 成功 | 使用有效 base_url/api_key/model 调用 test_connection | 返回 (True, "连接成功！模型回复：...") | | |


---

## 5. 同步引擎测试

> 测试对象：`sync_engine.py`（278 行，SyncEngine 类）
> 测试策略：测试同步流程完整性、增量逻辑、中断机制、回调事件、重分析流程

### 5.1 初始化与控制测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| SE-001 | 初始化 | SyncEngine 构造 | 传入 config dict 创建 SyncEngine | self.config 等于传入的 config，self._stop_event 未被 set | | |
| SE-002 | 控制 | stop 方法 | 调用 engine.stop() | engine.is_stopped() 返回 True | | |
| SE-003 | 控制 | is_stopped 初始状态 | 创建新 SyncEngine 后调用 is_stopped() | 返回 False | | |

### 5.2 同步流程测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| SE-004 | 同步 | 空 Token 同步 | 创建 config 无 github_token，调用 sync() | callback 收到 ('log', ...) 含 "Token 为空" 提示，无异常 | | |
| SE-005 | 同步 | 完整同步流程 | 配置有效 Token + LLM，调用 sync(max_workers=2, callback=cb) | callback 先收到 'log' 事件（获取列表），再收到 'progress' 事件，最终收到 'done' 事件 | | |
| SE-006 | 同步 | 增量同步跳过已有 | 1. 先同步一次 2. 再次同步 | 第二次 callback 日志显示 "无新增项目" 或新增数为 0 | | |
| SE-007 | 同步 | 同步中断 | 1. 开始同步 2. 立即调用 stop() | callback 最终收到 'done' 事件，已处理的项目入库，未处理的被跳过 | | |
| SE-008 | 同步 | 无 LLM 同步 | 配置有效 Token 但无 LLM 参数，调用 sync() | 项目被同步入库，summary 使用 description 兜底，category 为 "其他" | | |
| SE-009 | 同步 | callback 事件类型 | 同步过程中记录所有 callback 调用 | 至少包含 'log'、'progress'、'done' 三种事件类型 | | |

### 5.3 重新分析测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| SE-010 | 重分析 | 未配置 LLM | 不配置 LLM 参数，调用 reanalyze() | callback 日志显示 "未配置 LLM，无法重新分析" | | |


---

## 6. 数据分析测试

> 测试对象：`analysis.py`（217 行，相似推荐 + 用户对比 + 趋势分析）
> 测试策略：使用测试数据集验证 Jaccard 计算、对比逻辑、趋势统计正确性

### 6.1 相似项目推荐测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| AN-001 | 相似 | _tag_set 列表输入 | 调用 _tag_set(["LLM","Python","RAG"]) | 返回 {"llm","python","rag"}（小写集合） | | |
| AN-002 | 相似 | _tag_set JSON 字符串输入 | 调用 _tag_set('["A","B"]') | 返回 {"a","b"} | | |
| AN-003 | 相似 | _tag_set 空值 | 调用 _tag_set(None) 或 _tag_set([]) | 返回空集合 set() | | |
| AN-004 | 相似 | _jaccard 完全相同 | 调用 _jaccard({"a","b"}, {"a","b"}) | 返回 1.0 | | |
| AN-005 | 相似 | _jaccard 完全不同 | 调用 _jaccard({"a"}, {"b"}) | 返回 0.0 | | |
| AN-006 | 相似 | _jaccard 部分重叠 | 调用 _jaccard({"a","b","c"}, {"b","c","d"}) | 返回 2/4 = 0.5 | | |
| AN-007 | 相似 | _jaccard 双空集 | 调用 _jaccard(set(), set()) | 返回 0.0 | | |
| AN-008 | 相似 | find_similar_repos 基本功能 | 用测试数据对 id=100001 (AI项目, Python, LLM标签) 调用 find_similar_repos(top_n=3) | 返回 3 个最相似项目，100015 (另一个AI+Python+LLM) 应排第一，score > 0.5 | | |
| AN-009 | 相似 | find_similar_repos 不存在的 ID | 传入 target_id=999999 | 返回空列表 [] | | |
| AN-010 | 相似 | find_similar_repos 相同标签高分 | 100001 与 100015 共享 LLM/Python 标签和 AI 分类 | tag_sim=2/4=0.5, cat_sim=1.0, lang_sim=1.0, score=0.5*0.5+1.0*0.3+1.0*0.2=0.75 | | |
| AN-011 | 相似 | build_similar_map | 对测试数据调用 build_similar_map(top_n=2) | 返回 dict，key 为 repo_id，value 为 [{id, name, score}] 列表，长度 <= 2 | | |

### 6.2 用户 Star 对比测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| AN-012 | 对比 | compare_users 基本对比 | 对测试数据调用 compare_users(repos, "test-user", "other-user") | 返回 dict 含 only_a/only_b/common/stats_a/stats_b/user_a/user_b | | |
| AN-013 | 对比 | compare_users 统计验证 | 检查 stats_a["count"] 和 stats_b["count"] | stats_a.count = 14（test-user 的项目数），stats_b.count = 1（other-user 的项目数） | | |
| AN-014 | 对比 | compare_users 共同项目 | 检查 common 列表 | common 应为空列表（两用户无共同项目 ID） | | |

### 6.3 趋势分析测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| AN-015 | 趋势 | analyze_trends 基本功能 | 对测试数据调用 analyze_trends(repos, recent_days=30) | 返回 dict 含 recent_count/early_count/recent_categories/early_categories/rising_categories/new_tags/summary | | |


---

## 7. 导出模块测试

> 测试对象：`exporter.py`（266 行，5 个公开函数）
> 测试策略：验证四种格式导出正确性、统计计算准确性、筛选导出、模板选择

### 7.1 统计计算测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| EX-001 | 统计 | compute_stats 空列表 | 调用 compute_stats([]) | 返回 dict，total_count=0, avg_stars=0，各计数 dict 为空 | | |
| EX-002 | 统计 | compute_stats 正常数据 | 用 14 条可见测试数据调用 compute_stats | total_count=14, avg_stars=计算值，category_counts 含 14 个分类键，language_counts 含 Python/Go/TS 等 | | |
| EX-003 | 统计 | compute_stats 分类分布 | 检查 category_counts | "AI与大模型": 2（100001, 100015），"学习教程与资料": 1（100013）等 | | |
| EX-004 | 统计 | compute_stats 语言分布 | 检查 language_counts | "Python" 计数最高（100001,100007,100009,100013,100015 = 5 个） | | |
| EX-005 | 统计 | compute_stats Star 区间 | 检查 star_ranges | "10k+": 2（100013=50000, 100015=12000），"1k-10k": 若干，"0-100": 0 | | |
| EX-006 | 统计 | compute_stats 月度时间线 | 检查 monthly_timeline | 包含 "2026-01"/"2026-02" 等月份键，值为当月收藏数 | | |
| EX-007 | 统计 | compute_stats top_repos | 检查 top_repos | 列表长度 <= 10，按 stars 降序，第一项 name="test-user/python-tutorial"（50000 stars） | | |

### 7.2 HTML 导出测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| EX-008 | HTML | export_html 默认模板 | 调用 export_html()，不传参数 | 在 output/ 目录生成 index.html，文件大小 > 10KB | | |
| EX-009 | HTML | export_html compact 模板 | 调用 export_html(template_name="compact.html") | 在 output/ 目录生成 compact.html | | |
| EX-010 | HTML | export_html 自定义路径 | 调用 export_html(output_path="C:\\temp\\test.html") | 文件生成在指定路径 | | |
| EX-011 | HTML | export_html 筛选数据 | 传入仅含 2 个项目的 repos 列表 | 生成的 HTML 中 TOTAL_COUNT=2，repos_json 仅含 2 条 | | |

### 7.3 多格式导出测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| EX-012 | Markdown | export_markdown 基本导出 | 调用 export_markdown() | 生成 output/starmind_export.md，包含统计概览表和项目列表 | | |
| EX-013 | Markdown | export_markdown 内容验证 | 读取导出的 Markdown 文件 | 包含 "StarMind Manager 知识库导出" 标题、"项目总数" 统计、每个项目的 Star/分类/标签/摘要 | | |
| EX-014 | JSON | export_json 基本导出 | 调用 export_json() | 生成 output/starmind_export.json，JSON 格式合法 | | |
| EX-015 | JSON | export_json 内容验证 | 用 json.load 读取导出文件 | 含 exported_at(ISO 时间)/total_count/repos 三个键，repos 为列表 | | |
| EX-016 | CSV | export_csv 基本导出 | 调用 export_csv() | 生成 output/starmind_export.csv，UTF-8-BOM 编码 | | |
| EX-017 | CSV | export_csv 内容验证 | 用 csv.DictReader 读取导出文件 | 包含 id/name/stars/category/language/tags/summary/description/url/starred_at/owner_username 列头 | | |
| EX-018 | CSV | export_csv tags 分隔 | 检查 CSV 中 tags 列格式 | tags 用分号分隔（如 "LLM;RAG;Python"） | | |


---

## 8. GUI 测试

> 测试对象：`main.py`（946 行，StarMindApp 类）
> 测试策略：手动操作 CustomTkinter 界面，验证四个 Tab 的全部功能
> 前置条件：已运行测试数据准备脚本，数据库含 15 条测试数据

### 8.1 配置 Tab 测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| UI-001 | 配置 | 程序启动 | 运行 `python main.py` | 窗口标题为 "StarMind Manager"，尺寸约 1020x720，显示 4 个 Tab | | |
| UI-002 | 配置 | 配置 Tab 显示 | 点击 "配置" Tab | 显示 GitHub 配置区（Token/用户名输入框）、LLM 配置区（URL/Key/Model）、定时同步区 | | |
| UI-003 | 配置 | Token 输入 | 在 Token 输入框粘贴有效 Token | 输入框显示为密码掩码（圆点），文本不可见 | | |
| UI-004 | 配置 | Rate Limit 检测 | 1. 输入有效 Token 2. 点击 "检测 Rate Limit" | 标签显示 "剩余 XXXX / 5000"，颜色为绿色 | | |
| UI-005 | 配置 | Rate Limit 无 Token | 不输入 Token，点击 "检测 Rate Limit" | 标签显示 "请先填写 Token"，颜色为橙色 | | |
| UI-006 | 配置 | 获取 Token 链接 | 点击 "获取 Token" 按钮 | 浏览器打开 GitHub Token 创建页面 | | |
| UI-007 | 配置 | LLM 测试连通性 | 填写完整 LLM 参数后点击 "测试连通性" | 标签显示 "连接成功！模型回复：..."（绿色）或 "连接失败：..."（红色） | | |
| UI-008 | 配置 | LLM 连通未填参数 | 不填写 LLM 参数，点击 "测试连通性" | 标签显示 "请填写完整参数"（橙色） | | |
| UI-009 | 配置 | 保存配置 | 1. 填写所有配置 2. 点击 "保存配置" 3. 检查 config.json | config.json 更新为最新值，日志显示 "配置已保存" | | |
| UI-010 | 配置 | 配置恢复 | 1. 保存配置 2. 关闭程序 3. 重新启动 | 各输入框自动填入上次保存的值 | | |
| UI-011 | 配置 | 定时同步 | 勾选 "启用定时同步"，选择 "每天" | 状态显示 "将在 每天 后自动同步"（绿色） | | |
| UI-012 | 配置 | 定时同步禁用 | 取消勾选 "启用定时同步" | 状态显示 "已禁用"（灰色） | | |

### 8.2 任务 Tab 测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| UI-013 | 任务 | 任务 Tab 显示 | 点击 "任务" Tab | 显示并发数滑块（默认5）、开始/停止按钮、进度条、日志框 | | |
| UI-014 | 任务 | 滑块调整线程数 | 拖动滑块到 10 | 标签显示 "10" | | |
| UI-015 | 任务 | 开始同步 | 1. 配置好 Token 和 LLM 2. 点击 "开始同步" | 开始按钮变灰，停止按钮激活，日志框滚动显示同步进度 | | |
| UI-016 | 任务 | 同步进度显示 | 观察同步过程 | 进度条从 0 到 1 递增，统计标签显示 "进度: X/Y (Z%)" | | |
| UI-017 | 任务 | 同步完成通知 | 等待同步完成 | 弹出 messagebox 显示 "成功: X / 失败: Y" | | |
| UI-018 | 任务 | 停止同步 | 1. 开始同步 2. 点击 "停止" | 日志显示 "正在停止..."，同步在当前项目处理完后停止 | | |
| UI-019 | 任务 | 无 Token 同步 | 不配置 Token 点击 "开始同步" | 日志显示 "GitHub Token 为空，请先在配置填写"，不启动同步 | | |
| UI-020 | 任务 | 快捷键 Ctrl+S | 按 Ctrl+S | 触发开始同步（等同点击开始按钮） | | |
| UI-021 | 任务 | 快捷键 Esc | 同步中按 Esc | 触发停止同步（等同点击停止按钮） | | |

### 8.3 管理 Tab 测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| UI-022 | 管理 | 管理 Tab 显示 | 点击 "管理" Tab | 显示搜索框、分类下拉框、隐藏勾选框、项目列表表格、分页控件、批量操作栏、集合管理栏 | | |
| UI-023 | 管理 | 项目列表显示 | 观察项目列表 | 每行显示勾选框、项目名、Star 数、分类、标签、操作按钮（编辑/隐藏/收藏/删除） | | |
| UI-024 | 管理 | 搜索功能 | 在搜索框输入 "ai" 并按回车 | 项目列表仅显示名称/摘要中含 "ai" 的项目 | | |
| UI-025 | 管理 | 分类筛选 | 从分类下拉框选择 "AI与大模型" | 项目列表仅显示分类为 "AI与大模型" 的项目 | | |
| UI-026 | 管理 | 显示隐藏项目 | 勾选 "显示隐藏" | 列表中出现标记 "[hidden]" 的项目 | | |
| UI-027 | 管理 | 分页功能 | 1. page_size=50 2. 若超过 50 个项目 3. 点击 "下一页" | 页码标签更新，列表显示下一页数据 | | |
| UI-028 | 管理 | 编辑弹窗 | 点击某项目的编辑按钮(笔图标) | 弹出编辑窗口，显示摘要(文本框)、分类(下拉框)、标签(输入框)、备注(文本框) | | |
| UI-029 | 管理 | 编辑保存 | 1. 修改摘要为 "测试摘要" 2. 点击 "保存" | 状态显示 "已保存"（绿色），数据库中对应字段更新 | | |
| UI-030 | 管理 | AI 重新分析 | 在编辑弹窗中点击 "AI 重新分析" | 状态显示 "AI 重新分析中..."，完成后摘要/分类/标签被 AI 更新 | | |
| UI-031 | 管理 | 隐藏/恢复项目 | 点击隐藏按钮(眼睛图标) | 项目从列表消失（默认不显示隐藏）；勾选"显示隐藏"后可见，带 [hidden] 标记 | | |
| UI-032 | 管理 | 收藏切换 | 点击收藏按钮(星星图标) | 星星从空心变为实心（或反之），数据库 favorite 字段切换 | | |
| UI-033 | 管理 | 删除单个项目 | 1. 点击删除按钮(垃圾桶图标) 2. 确认对话框点"是" | 项目从数据库永久删除，列表刷新，导出 Tab 计数减少 | | |
| UI-034 | 管理 | 快捷键 Ctrl+K | 按 Ctrl+K | 焦点跳转到管理 Tab 的搜索框 | | |
| UI-035 | 管理 | 全选功能 | 勾选 "全选" 复选框 | 当前页所有项目被勾选 | | |
| UI-036 | 管理 | 批量删除 | 1. 勾选 3 个项目 2. 点击 "批量删除" 3. 确认 | 3 个项目被删除，列表刷新，计数更新 | | |
| UI-037 | 管理 | 批量隐藏 | 1. 勾选 2 个项目 2. 点击 "批量隐藏" | 2 个项目被设为隐藏 | | |
| UI-038 | 管理 | 批量改分类 | 1. 勾选 2 个项目 2. 从 "批量改分类" 下拉选择 "效率辅助工具" | 2 个项目分类变为 "效率辅助工具" | | |
| UI-039 | 管理 | 创建集合 | 1. 在 "新集合名" 输入框输入 "测试集合" 2. 点击 "创建" | 日志显示 "集合 [测试集合] 已创建"，集合下拉列表更新 | | |
| UI-040 | 管理 | 批量加入集合 | 1. 勾选 2 个项目 2. 从 "加入集合" 下拉选择 "测试集合" | 日志显示 "已将 2 个项目加入 [测试集合]" | | |
| UI-041 | 管理 | 查看集合 | 点击 "查看集合" | 弹出集合管理弹窗，左侧显示集合列表及项目数，右侧显示选中集合的项目 | | |

### 8.4 导出 Tab 测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| UI-042 | 导出 | 导出 Tab 显示 | 点击 "导出" Tab | 显示数据库项目数、导出格式单选按钮（5种）、导出按钮、导入区域 | | |
| UI-043 | 导出 | 刷新统计 | 点击 "刷新统计" | 标签更新为 "数据库中共 N 个项目 (隐藏: M)" | | |
| UI-044 | 导出 | 导出 HTML 卡片 | 1. 选择 "HTML 知识库（卡片视图）" 2. 点击 "导出" | 状态显示 "已导出到: .../index.html"，浏览器自动打开 | | |
| UI-045 | 导出 | 导出 HTML 列表 | 1. 选择 "HTML 知识库（列表视图）" 2. 点击 "导出" | 状态显示 "已导出到: .../compact.html"，浏览器自动打开 | | |
| UI-046 | 导出 | 导出 Markdown | 1. 选择 "Markdown 文档" 2. 点击 "导出" | 状态显示 "已导出到: .../starmind_export.md" | | |
| UI-047 | 导出 | 导出 JSON | 1. 选择 "JSON 数据（可备份恢复）" 2. 点击 "导出" | 状态显示 "已导出到: .../starmind_export.json" | | |
| UI-048 | 导出 | 导出 CSV | 1. 选择 "CSV 表格" 2. 点击 "导出" | 状态显示 "已导出到: .../starmind_export.csv" | | |
| UI-049 | 导出 | 空数据库导出 | 1. 清空数据库 2. 点击 "导出" | 状态显示 "数据库为空，请先执行同步"（橙色） | | |
| UI-050 | 导出 | 导入 JSON | 1. 点击 "选择文件导入" 2. 选择之前导出的 JSON 文件 | 日志显示 "JSON 导入完成，共 N 个项目"，项目数增加 | | |
| UI-051 | 导出 | 导入 CSV | 1. 点击 "选择文件导入" 2. 选择之前导出的 CSV 文件 | 日志显示 "CSV 导入完成，共 N 个项目"，项目数增加 | | |
| UI-052 | 导出 | 导入不支持格式 | 选择 .txt 文件导入 | 日志显示 "不支持的文件格式" | | |


---

## 9. CLI 测试

> 测试对象：`cli.py`（238 行，6 个子命令）
> 测试策略：在终端执行 CLI 命令，验证输出和副作用
> 前置条件：config.json 已配置有效 Token 和 LLM 参数

### 9.1 帮助与基本功能测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| CLI-001 | 基本 | 无参数执行 | 运行 `python cli.py` | 显示帮助信息，列出 sync/export/reanalyze/stats/search/compare 六个子命令 | | |
| CLI-002 | 基本 | --help 参数 | 运行 `python cli.py --help` | 显示 argparse 自动生成的帮助文本 | | |
| CLI-003 | 基本 | 子命令帮助 | 运行 `python cli.py sync --help` | 显示 sync 子命令的参数说明（-w/--workers） | | |

### 9.2 sync 子命令测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| CLI-004 | sync | 基本同步 | 运行 `python cli.py sync` | 终端显示获取 Star 列表日志、处理进度、最终显示 "完成: 成功 X, 失败 Y" | | |
| CLI-005 | sync | 指定线程数 | 运行 `python cli.py sync -w 3` | 使用 3 个并发线程执行同步 | | |
| CLI-006 | sync | 增量同步 | 同步完成后再运行 `python cli.py sync` | 显示 "无新增项目" 或 "已跳过 N 个已备份项目" | | |

### 9.3 export 子命令测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| CLI-007 | export | 默认 HTML 导出 | 运行 `python cli.py export` | 显示 "已导出到: .../output/index.html" | | |
| CLI-008 | export | JSON 导出 | 运行 `python cli.py export -f json` | 显示 "已导出到: .../output/starmind_export.json" | | |
| CLI-009 | export | CSV 导出 | 运行 `python cli.py export -f csv` | 显示 "已导出到: .../output/starmind_export.csv" | | |
| CLI-010 | export | Markdown 导出 | 运行 `python cli.py export -f markdown` | 显示 "已导出到: .../output/starmind_export.md" | | |
| CLI-011 | export | compact 模板 | 运行 `python cli.py export -t compact.html` | 显示 "已导出到: .../output/compact.html" | | |
| CLI-012 | export | 不支持格式 | 运行 `python cli.py export -f xml` | 显示 "不支持的格式: xml" | | |
| CLI-013 | export | 空数据库导出 | 1. 清空数据库 2. 运行 `python cli.py export` | 显示 "数据库为空，请先执行同步" | | |

### 9.4 reanalyze 子命令测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| CLI-014 | reanalyze | 分析全部 | 运行 `python cli.py reanalyze` | 显示 "开始重新分析 N 个项目..."，逐项显示进度 | | |
| CLI-015 | reanalyze | 分析指定 ID | 运行 `python cli.py reanalyze --ids 100001,100002` | 仅分析 id=100001 和 100002 两个项目 | | |

### 9.5 stats 子命令测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| CLI-016 | stats | 显示统计 | 运行 `python cli.py stats` | 显示总项目数、隐藏数、数据库大小、分类分布（柱状）、语言 Top 10、已同步用户、集合列表 | | |

### 9.6 search 子命令测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| CLI-017 | search | 基本搜索 | 运行 `python cli.py search Python` | 显示匹配项目列表（名称、Star、分类、语言、摘要） | | |
| CLI-018 | search | 带分类过滤 | 运行 `python cli.py search AI -c "AI与大模型"` | 仅显示分类为 "AI与大模型" 的匹配项目 | | |

### 9.7 compare 子命令测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| CLI-019 | compare | 用户对比 | 运行 `python cli.py compare test-user other-user` | 显示 "test-user 独有: X 个"、"other-user 独有: Y 个"、"共同: Z 个" | | |
| CLI-020 | compare | 空数据库对比 | 1. 清空数据库 2. 运行 compare 命令 | 显示 "数据库为空" | | |


---

## 10. HTML 知识库测试

> 测试对象：`templates/index.html`（380 行）和 `templates/compact.html`（137 行）
> 测试策略：在浏览器中打开导出的 HTML 文件，验证 Vue 3 渲染、Chart.js 图表、交互功能
> 前置条件：已通过 GUI 或 CLI 导出 HTML 文件

### 10.1 卡片视图 index.html 测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| HT-001 | 渲染 | 页面加载 | 在浏览器中打开 output/index.html | 页面加载完成无报错，导航栏显示 "StarMind" 和项目总数 | | |
| HT-002 | 渲染 | 卡片网格显示 | 观察主内容区 | 项目以卡片形式显示在 1/2/3 列网格中（响应式），每张卡片含项目名/Star/摘要/语言/分类/标签 | | |
| HT-003 | 渲染 | Star 数格式化 | 检查 Star >= 1000 的项目 | 显示为 "X.Xk" 格式（如 "15.0k"） | | |
| HT-004 | 渲染 | 分类标签显示 | 检查卡片上的分类标记 | 每张卡片显示分类名，带紫色背景 | | |
| HT-005 | 渲染 | 标签显示 | 检查卡片上的标签 | 最多显示 3 个标签，带 # 前缀，蓝色背景 | | |
| HT-006 | 渲染 | 收藏时间显示 | 检查卡片底部 | 显示 "收藏于 YYYY-MM-DD" 格式 | | |
| HT-007 | 渲染 | 集合标记 | 检查属于集合的项目卡片 | 底部显示 "文件夹: AI收藏" 等集合名 | | |

### 10.2 搜索与筛选测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| HT-008 | 搜索 | Fuse.js 模糊搜索 | 在搜索框输入 "python" | 卡片列表实时过滤，仅显示名称/摘要/标签含 "python" 的项目 | | |
| HT-009 | 搜索 | 搜索无结果 | 输入 "不存在的关键词xyz" | 显示 "未找到匹配的项目" 和 "清除筛选" 按钮 | | |
| HT-010 | 搜索 | 分类标签筛选 | 点击导航栏下方的 "AI与大模型" 分类标签 | 仅显示分类为 "AI与大模型" 的项目，标签高亮 | | |
| HT-011 | 搜索 | 标签点击筛选 | 点击卡片上的某个标签（如 #LLM） | 切换到标签筛选模式，页面顶部显示筛选标签和清除按钮 | | |
| HT-012 | 搜索 | 清除筛选 | 点击 "清除筛选" 或标签上的关闭按钮 | 搜索框清空，分类重置为 "全部"，显示所有项目 | | |
| HT-013 | 搜索 | 排序切换 | 从排序下拉框选择 "收藏时间" | 卡片按 starred_at 时间降序排列（最近收藏的在前） | | |

### 10.3 Dashboard 测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| HT-014 | Dashboard | 视图切换 | 点击导航栏的 "Dashboard" 按钮 | 切换到 Dashboard 视图，列表视图隐藏 | | |
| HT-015 | Dashboard | 统计卡片 | 查看 Dashboard 顶部的 4 个统计卡片 | 分别显示：总项目数（蓝色）、平均 Star（橙色）、最多分类（绿色）、最多语言（紫色） | | |
| HT-016 | Dashboard | 分类分布饼图 | 查看 Chart.js Doughnut 图表 | 显示各分类占比的环形图，图例在右侧，颜色区分清晰 | | |
| HT-017 | Dashboard | 语言分布条形图 | 查看 Chart.js 水平条形图 | 显示 Top 10 语言，横向条形图，Python 条最长 | | |
| HT-018 | Dashboard | Star 区间分布图 | 查看 Chart.js 柱状图 | 显示 0-100/100-1k/1k-10k/10k-100k/100k+ 五个区间 | | |
| HT-019 | Dashboard | 收藏时间线图 | 查看 Chart.js 折线图 | 显示按月统计的收藏数量曲线，X 轴为月份，Y 轴为数量 | | |
| HT-020 | Dashboard | 标签云 | 查看标签云区域 | 显示 Top 40 标签，高频标签更大更粗，点击标签跳转到列表视图并筛选 | | |
| HT-021 | Dashboard | 趋势分析 | 查看趋势分析区域 | 显示近期收藏数、趋势方向（分类增长百分比）、新兴标签列表 | | |
| HT-022 | Dashboard | Top 10 项目 | 查看 Top 10 区域 | 显示 Star 最多的 10 个项目，含排名/名称/Star/分类 | | |

### 10.4 详情弹窗测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| HT-023 | 弹窗 | 打开详情 | 在卡片视图中点击任意项目卡片 | 弹出模态弹窗，显示项目详情 | | |
| HT-024 | 弹窗 | 详情内容 | 查看弹窗内容 | 显示：项目名（可点击链接）、Star、语言、分类、所有标签、收藏时间、AI 摘要全文 | | |
| HT-025 | 弹窗 | 所属集合 | 查看弹窗中的集合信息 | 如果项目属于集合，显示 "所属集合: AI收藏, ..." | | |
| HT-026 | 弹窗 | 相似项目 | 查看弹窗中的相似项目区域 | 显示最多 3 个相似项目，含名称和相似度百分比 | | |
| HT-027 | 弹窗 | 相似项目跳转 | 点击相似项目列表中的某项 | 弹窗切换到该相似项目的详情 | | |
| HT-028 | 弹窗 | 标签跳转 | 点击弹窗中的某个标签 | 关闭弹窗，切换到列表视图，按该标签筛选 | | |
| HT-029 | 弹窗 | 关闭弹窗 | 点击弹窗右上角关闭按钮或点击背景遮罩 | 弹窗关闭 | | |
| HT-030 | 弹窗 | Esc 关闭弹窗 | 弹窗打开时按 Esc 键 | 弹窗关闭 | | |

### 10.5 暗色模式与快捷键测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| HT-031 | 暗色 | 暗色模式切换 | 点击导航栏的月亮/太阳图标 | 页面切换为暗色/亮色主题，背景变深色，文字变浅色 | | |
| HT-032 | 暗色 | 暗色下图表 | 在暗色模式下查看 Dashboard 图表 | 图表文字颜色适配暗色主题，网格线变暗 | | |
| HT-033 | 快捷键 | Ctrl+K 聚焦搜索 | 按 Ctrl+K | 搜索输入框获得焦点 | | |
| HT-034 | 快捷键 | / 键聚焦搜索 | 非输入状态按 / 键 | 搜索输入框获得焦点 | | |
| HT-035 | 快捷键 | D 键切换视图 | 非输入状态按 D 键 | 在列表视图和 Dashboard 之间切换 | | |

### 10.6 compact.html 列表视图测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| HT-036 | 紧凑 | compact.html 加载 | 在浏览器中打开 output/compact.html | 页面加载完成，显示紧凑列表视图 | | |
| HT-037 | 紧凑 | 列表布局 | 观察列表结构 | 每行显示：Star 数（右侧对齐）、项目名+摘要、语言、分类、标签（最多 2 个） | | |
| HT-038 | 紧凑 | 搜索功能 | 在搜索框输入关键词 | Fuse.js 实时过滤列表 | | |
| HT-039 | 紧凑 | 分类筛选 | 点击分类标签 | 列表仅显示该分类项目 | | |
| HT-040 | 紧凑 | 标签点击 | 点击行内标签 | 进入标签筛选模式 | | |
| HT-041 | 紧凑 | 暗色模式 | 点击暗色切换按钮 | 主题切换正常 | | |
| HT-042 | 紧凑 | 项目链接 | 点击列表中的项目 | 在新标签页打开 GitHub 仓库页面 | | |


---

## 11. 端到端集成测试

> 测试策略：模拟真实用户完整操作流程，覆盖从同步到导出的全链路
> 前置条件：干净数据库、有效 Token 和 LLM 配置

### 11.1 完整同步到导出流程

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| E2E-001 | 全流程 | GUI 完整流程 | 1. 启动 main.py 2. 配置 Tab 填写 Token/LLM 3. 保存配置 4. 任务 Tab 点击开始同步 5. 等待同步完成 6. 管理 Tab 查看项目列表 7. 编辑某个项目 8. 创建集合并添加项目 9. 导出 Tab 导出 HTML 10. 浏览器验证 HTML | 全流程无报错，每步操作结果正确，最终 HTML 包含所有同步的项目 | | |
| E2E-002 | 全流程 | CLI 完整流程 | 1. `python cli.py sync` 2. `python cli.py stats` 3. `python cli.py search Python` 4. `python cli.py export -f json` 5. `python cli.py export -f csv` 6. `python cli.py export` 7. 浏览器打开 HTML | 全部命令执行无报错，导出文件内容正确 | | |
| E2E-003 | 全流程 | 增量同步+重分析 | 1. 首次同步 2. 管理 Tab 中对某个项目点击 "AI 重新分析" 3. 验证摘要/分类/标签被更新 4. 再次同步（增量） 5. 导出 HTML | 重分析后元数据更新，增量同步不覆盖手动编辑，HTML 反映最新数据 | | |
| E2E-004 | 全流程 | 多格式导出验证 | 1. 同步后导出全部 5 种格式 2. 逐一打开验证 | index.html（卡片）可浏览且含图表；compact.html（列表）可浏览；Markdown 含项目列表；JSON 可被 json.load 解析；CSV 可被 Excel 打开 | | |
| E2E-005 | 全流程 | 导入导出闭环 | 1. 导出 JSON 2. 清空数据库 3. 导入 JSON 4. 对比项目数和内容 | 导入后项目数与导出前一致，各字段（含 tags/category/summary）数据一致 | | |
| E2E-006 | 全流程 | CSV 导入导出闭环 | 1. 导出 CSV 2. 清空数据库 3. 导入 CSV 4. 对比项目数 | 导入后项目数一致，tags 字段正确解析（分号分隔还原为列表） | | |

### 11.2 集合管理完整流程

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| E2E-007 | 集合 | 集合完整流程 | 1. 管理 Tab 创建 "前端项目" 集合 2. 勾选 react-ui 项目 3. 批量加入集合 4. 查看集合确认 5. 导出 HTML 6. 在 HTML 中检查集合标记 | 集合创建成功，项目正确归入集合，HTML 中该项目卡片显示集合名 | | |
| E2E-008 | 集合 | 集合删除级联 | 1. 创建 "临时集合" 2. 添加项目 3. 查看集合弹窗删除集合 4. 导出 JSON 检查 | 集合和关联记录均被删除 | | |

### 11.3 批量操作流程

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| E2E-009 | 批量 | 批量操作组合 | 1. 勾选 3 个项目 2. 批量改分类为 "效率辅助工具" 3. 勾选另外 2 个 4. 批量隐藏 5. 勾选 "显示隐藏" 查看 6. 导出 JSON 验证 | 被改分类的项目 category 更新，被隐藏的项目 hidden=1，JSON 中反映这些变更 | | |
| E2E-010 | 批量 | 批量删除不可逆 | 1. 勾选 2 个项目 2. 批量删除 3. 确认 4. 查看列表 | 项目永久消失，不可恢复 | | |


---

## 12. 边界条件与异常测试

> 测试策略：验证各模块在极端输入和异常场景下的健壮性

### 12.1 空数据库测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| BD-001 | 空数据 | 空数据库 get_all_repos | 删除 starmind.db 后调用 get_all_repos() | 返回空列表 []，不抛异常 | | |
| BD-002 | 空数据 | 空数据库搜索 | 调用 search_repos(query="test") | 返回空列表 [] | | |
| BD-003 | 空数据 | 空数据库分页 | 调用 get_repos_paged(page=1) | 返回 ([], 0) | | |
| BD-004 | 空数据 | 空数据库导出 HTML | 调用 export_html() | 不抛异常，生成 HTML 文件（可能项目列表为空） | | |
| BD-005 | 空数据 | 空数据库导出 Markdown | 调用 export_markdown() | 生成文件，统计部分 total_count=0 | | |
| BD-006 | 空数据 | 空数据库 CLI stats | 运行 `python cli.py stats` | 显示 "数据库为空" | | |
| BD-007 | 空数据 | 空数据库 CLI search | 运行 `python cli.py search test` | 显示 "未找到匹配项目" | | |
| BD-008 | 空数据 | 空数据库相似度 | 调用 find_similar_repos([], 1) | 返回空列表 [] | | |
| BD-009 | 空数据 | 空数据库趋势分析 | 调用 analyze_trends([]) | summary 为 "暂无足够数据进行趋势分析" | | |
| BD-010 | 空数据 | 空数据库用户对比 | 调用 compare_users([], "a", "b") | only_a/only_b/common 均为空列表 | | |

### 12.2 无效输入测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| BD-011 | 无效输入 | SQL 注入防护 | 调用 search_repos(query="'; DROP TABLE starred_repos; --") | 返回空列表或不匹配项，数据库表不受影响 | | |
| BD-012 | 无效输入 | 超长摘要输入 | 调用 update_repo_metadata(100001, summary="A"*10000) | 不抛异常，摘要字段被截断或完整存储 | | |
| BD-013 | 无效输入 | 特殊字符标签 | 调用 update_repo_metadata(100001, tags=["<script>alert(1)</script>", "normal"]) | 存储成功，HTML 导出时 jinja2 autoescape 转义特殊字符 | | |
| BD-014 | 无效输入 | Unicode 标签 | 调用 update_repo_metadata(100001, tags=["深度学习", "機器学習", "ML"]) | 正确存储和读取 Unicode 标签 | | |
| BD-015 | 无效输入 | 空字符串参数 | 调用 upsert_repo({"id": 999, "name": ""}) | 不抛异常，记录以空 name 存入 | | |

### 12.3 网络异常测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| BD-016 | 网络 | 无网络同步 | 1. 断开网络 2. 点击开始同步 | 日志显示网络错误，不崩溃，同步正常终止 | | |
| BD-017 | 网络 | Token 过期 | 使用已失效 Token 同步 | 日志显示 401 错误，同步正常终止 | | |
| BD-018 | 网络 | LLM 不可用 | 配置无效 LLM 地址，执行同步 | 项目同步入库但 summary 使用 description 兜底，category 为 "其他" | | |


---

## 13. 兼容性测试

> 测试策略：验证旧数据库迁移、配置文件向后兼容性、跨平台路径处理

### 13.1 数据库迁移测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| CP-001 | 迁移 | v1.0 数据库升级 | 1. 创建只有基础列（无 hidden/notes/favorite）的旧表 2. 插入数据 3. 调用 init_db() 4. 查询数据 | 新列自动添加（hidden=0, notes=NULL, favorite=0），旧数据完整保留 | | |
| CP-002 | 迁移 | 无 owner_username 列的数据库 | 1. 创建不含 owner_username 列的旧表 2. 插入数据 3. 调用 init_db() | 列自动添加，旧数据中 owner_username 为 NULL | | |
| CP-003 | 迁移 | 无 starred_at 列的数据库 | 同上方式测试 starred_at 列迁移 | 列自动添加，旧数据中 starred_at 为 NULL | | |
| CP-004 | 迁移 | 无集合表的数据库 | 1. 有 starred_repos 表但无 collections/collection_items 2. 调用 init_db() | 集合表自动创建，starred_repos 数据不受影响 | | |

### 13.2 配置文件兼容测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| CP-005 | 配置 | 旧版 config.json | 1. 创建只含 github_token 的旧版 config.json 2. 启动程序 | 程序正常启动，缺失字段使用默认值（空字符串/False） | | |
| CP-006 | 配置 | config.json 不存在 | 1. 删除 config.json 2. 启动程序 | 程序正常启动，所有配置字段为空/默认值 | | |
| CP-007 | 配置 | config.json 格式错误 | 1. 写入非法 JSON 到 config.json 2. 启动程序 | 程序正常启动（try-except 捕获），使用空配置 | | |
| CP-008 | 配置 | config.json 含额外字段 | 1. 在 config.json 中添加 "extra_key": "value" 2. 启动程序 | 程序正常启动，额外字段被忽略 | | |

---

## 14. 性能测试

> 测试策略：验证大数据量场景下的响应速度和资源使用

### 14.1 大数据量测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| PF-001 | 大数据 | 1000 条记录查询 | 1. 批量插入 1000 条记录 2. 调用 get_all_repos() | 返回 1000 条记录，耗时 < 1 秒 | | |
| PF-002 | 大数据 | 1000 条记录分页 | 1. 同上数据量 2. 调用 get_repos_paged(page=1, page_size=50) | 返回 50 条记录，耗时 < 0.5 秒 | | |
| PF-003 | 大数据 | 1000 条记录搜索 | 调用 search_repos(query="Python") | 返回匹配结果，耗时 < 1 秒 | | |
| PF-004 | 大数据 | 1000 条记录 HTML 导出 | 调用 export_html() 生成包含 1000 个项目的 HTML | 文件生成完成，耗时 < 30 秒（含相似度计算） | | |
| PF-005 | 大数据 | 1000 条记录 JSON 导出 | 调用 export_json() | 文件生成完成，耗时 < 5 秒 | | |
| PF-006 | 大数据 | 相似度计算性能 | 1. 500 条数据 2. 调用 build_similar_map(top_n=3) | 计算完成，耗时 < 60 秒（500*500 次比较） | | |

### 14.2 GUI 响应性测试

| 编号 | 类别 | 描述 | 测试步骤 | 预期结果 | 实际结果 | 状态 |
|------|------|------|---------|---------|---------|------|
| PF-007 | GUI | 同步期间 GUI 不卡顿 | 1. 开始同步 2. 尝试切换 Tab、滚动列表 | GUI 保持响应，不出现 "未响应" 状态 | | |
| PF-008 | GUI | 管理 Tab 大列表加载 | 1. 数据库含 500+ 项目 2. 打开管理 Tab | 分页加载正常，列表滚动流畅 | | |
| PF-009 | GUI | 导出期间 GUI 不卡顿 | 1. 大数据量 2. 点击导出 | 导出在后台线程执行，GUI 保持响应 | | |

---

## 15. Bug 追踪表

> 用于记录测试过程中发现的所有缺陷

| 编号 | 发现日期 | 关联用例 | 严重级别 | 描述 | 复现步骤 | 预期行为 | 实际行为 | 状态 | 修复日期 | 备注 |
|------|---------|---------|---------|------|---------|---------|---------|------|---------|------|
| BUG-001 | | | | | | | | 待确认 | | |
| BUG-002 | | | | | | | | 待确认 | | |
| BUG-003 | | | | | | | | 待确认 | | |
| BUG-004 | | | | | | | | 待确认 | | |
| BUG-005 | | | | | | | | 待确认 | | |

### 严重级别定义

| 级别 | 含义 | 说明 |
|------|------|------|
| P0-致命 | 数据丢失/崩溃 | 程序崩溃、数据库损坏、数据不可恢复丢失 |
| P1-严重 | 核心功能不可用 | 同步失败、导出失败、数据不一致 |
| P2-一般 | 功能异常但有变通方案 | 显示错误、筛选不准确、UI 样式问题 |
| P3-轻微 | 体验问题 | 提示信息不准确、对齐问题、多余操作步骤 |

### Bug 状态流转

```
待确认 -> 已确认 -> 修复中 -> 已修复 -> 已验证 -> 关闭
                  -> 不修复（需注明原因）
                  -> 延期（需注明计划版本）
```

---

## 附录 A：测试覆盖矩阵

| 功能编号 | 功能名称 | 对应测试用例 | 覆盖状态 |
|---------|---------|-------------|---------|
| F-01 | 重新分析项目 | SE-010, UI-030 | |
| F-02 | 手动编辑元数据 | DB-037~DB-042, UI-028~UI-029 | |
| F-03 | 自定义集合 | DB-051~DB-066, UI-039~UI-041, E2E-007~E2E-008 | |
| F-04 | 删除/隐藏项目 | DB-030~DB-036, UI-031~UI-033 | |
| F-05 | 批量操作 | DB-043~DB-050, UI-035~UI-038, E2E-009~E2E-010 | |
| F-06 | 分类分布饼图 | HT-016 | |
| F-07 | 语言分布条形图 | HT-017 | |
| F-08 | Star 区间分布图 | HT-018 | |
| F-09 | 收藏时间线图 | HT-019 | |
| F-10 | Dashboard 综合面板 | HT-014~HT-022 | |
| F-11 | 多格式导出 | EX-008~EX-018, CLI-007~CLI-012, UI-044~UI-048 | |
| F-12 | 筛选导出 | EX-011 | |
| F-13 | 自定义模板 | HT-036~HT-042, EX-009, CLI-011 | |
| F-14 | 相似项目推荐 | AN-001~AN-011, HT-026~HT-028 | |
| F-15 | 用户 Star 对比 | AN-012~AN-014, CLI-019~CLI-020 | |
| F-16 | 趋势分析 | AN-015, HT-021 | |
| F-17 | 定时同步 | UI-011~UI-012 | |
| F-18 | CLI 模式 | CLI-001~CLI-020 | |
| F-19 | 同步完成通知 | UI-017 | |
| F-20 | 项目详情弹窗 | HT-023~HT-030 | |
| F-21 | 标签云 | HT-020 | |
| F-22 | 高级搜索 | HT-008~HT-013, DB-072~DB-078 | |
| F-23 | 键盘快捷键 | UI-020~UI-021, UI-034, HT-033~HT-035 | |
| F-24 | 导入/恢复 | UI-050~UI-052, E2E-005~E2E-006 | |

---

## 附录 B：测试数据恢复

测试完成后恢复原始环境：

| 步骤 | 操作 |
|------|------|
| 1 | 删除测试期间生成的 starmind.db |
| 2 | 将 starmind.db.bak 恢复为 starmind.db |
| 3 | 将 config.json.bak 恢复为 config.json |
| 4 | 删除 output/ 目录下的测试导出文件 |
| 5 | 删除临时 Python 脚本文件（write_part*.py） |

---

> 文档结束 | 总计测试用例：约 200+ 条 | 覆盖全部 24 项功能

