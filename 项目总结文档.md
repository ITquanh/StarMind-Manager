# StarMind Manager v2.0 — 项目总结文档

> 最后更新：2026-06-06 | 项目路径：`GetGithub`

---

## 一、项目概述

**StarMind Manager** 是一款面向开发者的 **GitHub Star 资产智能管理工具**。它通过 GitHub API 获取用户收藏的 Star 项目，接入大语言模型（LLM）自动生成中文摘要、技术标签和智能分类，最终导出为**支持全文搜索、数据可视化、详情弹窗的离线 HTML 知识库**。

**v2.0 新增**：数据管理面板、集合分组、批量操作、Chart.js 数据仪表盘、多格式导出、CLI 命令行、定时同步、相似推荐、趋势分析、快捷键、导入/恢复等 24 项新功能。

---

## 二、技术架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     main.py (GUI 入口)                           │
│               CustomTkinter 桌面应用 (1020×720)                    │
│    ┌──────────┬──────────┬──────────┬──────────┐                 │
│    │⚙️ 配置Tab │🚀 任务Tab │📋 管理Tab │📤 导出Tab │                 │
│    └────┬─────┴────┬─────┴────┬─────┴────┬─────┘                 │
│         │          │          │          │                       │
│    ┌────▼────┐ ┌───▼────┐ ┌──▼───┐ ┌───▼──────┐                │
│    │github_  │ │  llm   │ │  db  │ │ exporter │                │
│    │  api    │ │(OpenAI │ │(SQLit│ │(Jinja2   │                │
│    │(REST)   │ │ 兼容)  │ │  e3) │ │→HTML/MD/ │                │
│    └────┬────┘ └───┬────┘ │      │ │ JSON/CSV)│                │
│         │          │      └──┬───┘ └───┬──────┘                │
│         │          └─────┬──┘          │                       │
│    ┌────▼────────────────▼───┐   ┌────▼──────────┐            │
│    │    sync_engine.py       │   │  analysis.py  │            │
│    │  (同步/重分析引擎)       │   │ (相似/对比/   │            │
│    │  GUI+CLI 共用            │   │  趋势分析)    │            │
│    └─────────────────────────┘   └───────────────┘            │
└─────────────────────────────────────────────────────────────────┘
         │                                        │
         ▼                                        ▼
┌──────────────┐                   ┌──────────────────────┐
│   cli.py     │                   │ templates/index.html │
│ (命令行入口) │                   │  Vue3 + Tailwind CSS │
│ 6个子命令     │                   │  + Fuse.js + Chart.js│
└──────────────┘                   │  + 暗黑模式           │
                                   ├──────────────────────┤
                                   │templates/compact.html│
                                   │  (紧凑列表视图)       │
                                   └──────────────────────┘
```

---

## 三、模块详解

### 3.1 `main.py` — 主程序 & GUI 入口（946 行）

| 职责 | 说明 |
|------|------|
| **GUI 框架** | CustomTkinter 暗色主题桌面应用，窗口 1020×720 |
| **四 Tab 布局** | ⚙️ 配置 → 🚀 任务 → 📋 管理 → 📤 导出 |
| **配置管理** | 读写 `config.json`，持久化 Token、LLM 参数、定时同步设置 |
| **同步引擎** | 委托 `SyncEngine`，事件回调驱动 UI 更新 |
| **管理面板** | 分页表格（50条/页）、编辑弹窗、集合弹窗、批量操作 |
| **多格式导出** | HTML卡片/HTML列表/Markdown/JSON/CSV 一键切换 |
| **数据导入** | 支持 JSON（备份恢复）和 CSV 格式导入 |
| **定时同步** | 可配置 每小时/每天/每周 自动同步 |
| **快捷键** | Ctrl+S 同步、Escape 停止、Ctrl+K 搜索 |

**四 Tab 详解**：

| Tab | 功能 |
|-----|------|
| ⚙️ 配置 | GitHub Token + LLM 参数 + 定时同步开关 + 连通性测试 |
| 🚀 任务 | 并发滑块 + 开始/停止 + 进度条 + 日志 |
| 📋 管理 | 分页表格 + 搜索/筛选 + 编辑弹窗 + 集合管理 + 批量操作 |
| 📤 导出 | 格式选择（5种）+ 导出按钮 + 导入功能（JSON/CSV） |

### 3.2 `db.py` — 数据库模块（525 行）

**数据库**：SQLite3，文件 `starmind.db`

**表 `starred_repos`**（15 个字段）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | INTEGER PK | GitHub 仓库 ID |
| `name` | TEXT NOT NULL | 仓库全名 |
| `stars` | INTEGER | Star 数量 |
| `summary` | TEXT | AI 生成中文摘要 |
| `category` | TEXT | 智能分类（15类） |
| `tags` | TEXT | JSON 数组，技术标签 |
| `language` | TEXT | 主要编程语言 |
| `url` | TEXT | 仓库链接 |
| `description` | TEXT | GitHub 原始描述 |
| `processed_date` | TEXT | 处理时间戳 |
| `owner_username` | TEXT | Star 归属用户 |
| `starred_at` | TEXT | 收藏时间 |
| `hidden` | INTEGER | 软删除标记（0/1） |
| `notes` | TEXT | 用户备注 |
| `favorite` | INTEGER | 收藏标记（0/1） |

**表 `collections`**：自定义集合（id, name, created_at）

**表 `collection_items`**：集合-项目关联（collection_id, repo_id）

**28 个函数**：

| 分类 | 函数 |
|------|------|
| 基础 CRUD | `upsert_repo`, `get_all_repos`, `get_repos_paged`, `get_repo_by_id`, `get_repo_count`, `get_existing_ids` |
| 软删除 | `toggle_hidden`, `hard_delete_repo` |
| 收藏 | `toggle_favorite` |
| 编辑 | `update_repo_metadata` |
| 批量 | `batch_toggle_hidden`, `batch_delete`, `batch_update_category`, `batch_add_to_collection` |
| 集合 | `create_collection`, `delete_collection`, `get_collections`, `add_to_collection`, `remove_from_collection`, `get_collection_repos`, `get_repo_collections` |
| 查询 | `get_all_categories`, `get_usernames`, `get_repos_by_username`, `search_repos` |

### 3.3 `sync_engine.py` — 同步引擎（278 行）

从 `main.py` 提取的核心逻辑，GUI 和 CLI 共用：

```python
class SyncEngine:
    def sync(max_workers, callback)       # Star 同步
    def reanalyze(repo_ids, max_workers, callback)  # 重新分析
    def stop()                            # 停止
```

**回调事件**：`('log', msg)`, `('progress', done, total)`, `('done', success, fail)`

### 3.4 `github_api.py` — GitHub API 模块（193 行，未变）

| 功能 | API 端点 |
|------|----------|
| 获取 Star 列表 | `GET /user/starred` (分页，star+json header) |
| 获取 Readme | `GET /repos/{name}/readme` (Base64 解码) |
| 获取文件树 | `GET /repos/{name}/contents/` |
| 获取仓库详情 | `GET /repos/{name}` |
| 检测 Rate Limit | `GET /rate_limit` |

### 3.5 `llm.py` — LLM 分析模块（168 行，未变）

三级内容降级：Readme → Description → 文件树 → None

### 3.6 `exporter.py` — 多格式导出模块（266 行）

| 函数 | 输出 |
|------|------|
| `export_html(repos, template_name)` | HTML 知识库（含图表/标签云/相似度/趋势） |
| `export_markdown(repos)` | Markdown 文档 |
| `export_json(repos)` | JSON（可用于备份恢复） |
| `export_csv(repos)` | CSV 表格（tags 分号分隔） |
| `compute_stats(repos)` | 统计数据（分类/语言/Star区间/时间线） |

**HTML 导出注入数据**：
- `repos_json` — 项目数据
- `categories_json` / `tags_json` — 筛选项
- `stats_json` — Chart.js 图表数据
- `similar_map_json` — 相似项目映射
- `collections_map_json` — 项目-集合映射
- `trends_json` — 趋势分析数据

### 3.7 `analysis.py` — 数据分析模块（217 行）

| 函数 | 功能 |
|------|------|
| `find_similar_repos(repos, target_id, top_n)` | 基于 tags+category+language 的 Jaccard 相似度推荐 |
| `build_similar_map(repos, top_n)` | 预计算所有项目的相似映射 |
| `compare_users(repos, user_a, user_b)` | 两个用户的 Star 差异对比 |
| `analyze_trends(repos, recent_days)` | 收藏趋势分析（分类变化、新兴标签） |

### 3.8 `cli.py` — CLI 命令行（238 行）

```
python cli.py sync [-w 5]              # 同步
python cli.py export [-f html|json|csv|markdown] [-t compact.html]  # 导出
python cli.py reanalyze [--ids 1,2,3] [-w 3]   # 重分析
python cli.py stats                    # 统计
python cli.py search "keyword" [-c category] [-l language]  # 搜索
python cli.py compare user_a user_b    # 对比
```

### 3.9 `templates/index.html` — 卡片视图模板（380 行）

**技术栈**：Vue 3 + Tailwind CSS + Fuse.js + Chart.js

**功能**：
- 🔍 Fuse.js 模糊搜索
- 📂 分类 pill + 🏷️ 标签点击筛选
- 📊 Dashboard 视图（4个 Chart.js 图表 + 统计卡片 + 标签云 + 趋势分析）
- 🔗 项目详情弹窗（摘要、标签、集合、相似项目）
- 📈 排序（Star/收藏时间）
- 🌙 暗黑模式 + ⌨️ 快捷键（Ctrl+K, Esc, D）

### 3.10 `templates/compact.html` — 紧凑列表模板（137 行）

表格布局（每行一个项目），支持搜索/分类/标签筛选，轻量级替代方案。

---

## 四、配置与运行

### 4.1 依赖（`requirements.txt`）

```
customtkinter>=5.2.0    # GUI 框架
requests>=2.31.0        # HTTP 客户端
openai>=1.12.0          # LLM SDK
jinja2>=3.1.3           # 模板引擎
```

**CDN 依赖（HTML 知识库中）**：Vue 3, Tailwind CSS, Fuse.js, Chart.js, Google Fonts

### 4.2 运行方式

```bash
# GUI 模式
python main.py

# CLI 模式
python cli.py sync
python cli.py export -f json
python cli.py stats
```

### 4.3 配置文件（`config.json`）

```json
{
  "github_token": "ghp_xxx",
  "github_username": "",
  "llm_base_url": "http://127.0.0.1:11434/v1",
  "llm_api_key": "xxx",
  "llm_model": "xxx",
  "schedule_enabled": false,
  "schedule_interval": "每天"
}
```

---

## 五、代码统计

| 文件 | 行数 | 职责 |
|------|------|------|
| `main.py` | 946 | GUI (4 Tab) + 编辑/集合弹窗 + 批量操作 + 定时同步 + 导入 |
| `db.py` | 525 | SQLite 持久化 (3表) + 28个函数 |
| `github_api.py` | 193 | GitHub REST API 封装 |
| `llm.py` | 168 | LLM 调用与分析 |
| `sync_engine.py` | 278 | 同步/重分析引擎 (GUI+CLI 共用) |
| `analysis.py` | 217 | 相似推荐 + 用户对比 + 趋势分析 |
| `exporter.py` | 266 | 多格式导出 + 统计计算 |
| `cli.py` | 238 | CLI 命令行入口 (6子命令) |
| `templates/index.html` | 380 | 卡片视图 + Dashboard + 详情弹窗 |
| `templates/compact.html` | 137 | 紧凑列表视图 |
| **合计** | **3,348** | — |

---

## 六、版本演进

| 提交 | 说明 |
|------|------|
| `bd5ca43` | **v1.0** — 初版：GUI / 同步 / LLM / HTML 导出 |
| `52e69c7` | **v1.1** — 增量同步 + 进度恢复 |
| `ee38f3f` ~ `4a95041` | v1.x 修复：429退避、线程假死、截断优化 |
| `53cefea` ~ `6c07c3a` | 打包发布 |
| `9a00557` | 项目总结文档 |
| `2a4c2d7` | **v2.0** — 24项新功能全面扩展 |

### v2.0 完整功能清单（24 项）

**A. 数据管理增强**
1. ✅ 重新分析已有项目（刷新 AI 摘要）
2. ✅ 手动编辑标签/分类/摘要/备注
3. ✅ 自定义收藏夹/集合分组
4. ✅ 删除/隐藏项目（软删除 + 硬删除）
5. ✅ 批量操作（删除、隐藏、改分类、加入集合）

**B. 数据可视化**
6. ✅ 分类分布饼图 (Chart.js Doughnut)
7. ✅ 语言分布统计图 (Chart.js Bar)
8. ✅ Star 数量区间分布图
9. ✅ 收藏时间线趋势图 (Chart.js Line)
10. ✅ 综合数据 Dashboard

**C. 导出增强**
11. ✅ 多格式导出 (Markdown / JSON / CSV)
12. ✅ 筛选后导出（管理 Tab 筛选）
13. ✅ 自定义导出模板（compact.html 紧凑列表视图）

**D. 搜索与发现**
14. ✅ 相似项目推荐（Jaccard 相似度）
15. ✅ 用户间 Star 对比
16. ✅ 趋势分析（分类变化、新兴标签）

**E. 自动化与集成**
17. ✅ 定时自动同步（每小时/每天/每周）
18. ✅ CLI 命令行模式（6 子命令）
19. ✅ 同步完成弹窗通知

**F. 用户体验优化**
20. ✅ 项目详情弹窗（摘要/标签/集合/相似项目）
21. ✅ 标签云展示
22. ✅ 高级搜索（Fuse.js + 分类/标签组合）
23. ✅ 快捷键（Ctrl+S/Esc/Ctrl+K）
24. ✅ 导入/恢复（JSON/CSV）

---

## 七、潜在改进方向

| 方向 | 建议 |
|------|------|
| **安全性** | `config.json` 明文 Token，建议加密 |
| **测试覆盖** | 补充单元测试 |
| **国际化** | LLM Prompt 多语言选项 |
| **多数据源** | 扩展支持 GitLab、Gitee |
| **离线打包** | CDN 资源内联（Vue/Tailwind/Chart.js） |
| **性能** | 超大数据集优化（虚拟滚动、分页标签云） |

---

*文档由 Claude 自动生成，基于对项目全部源码的完整阅读。*
