# StarMind Manager — 项目总结文档

> 自动生成于 2026-06-05 | 项目路径：`GetGithub`

---

## 一、项目概述

**StarMind Manager** 是一款面向开发者的 **GitHub Star 资产智能管理工具**。它通过 GitHub API 获取用户收藏的 Star 项目，接入大语言模型（LLM）自动生成中文摘要、技术标签和智能分类，最终导出为一个**支持全文搜索的离线 HTML 知识库**。

**一句话定位**：把散乱的 GitHub Stars 变成可搜索、可分类的中文知识库。

---

## 二、技术架构

```
┌──────────────────────────────────────────────────────┐
│                   main.py (GUI 入口)                  │
│               CustomTkinter 桌面应用                   │
│         ┌───────────┬───────────┬──────────┐          │
│         │ ⚙️ 配置 Tab │ 🚀 任务 Tab │ 📤 导出 Tab │          │
│         └─────┬─────┴─────┬─────┴────┬─────┘          │
│               │           │          │                │
│    ┌──────────▼──┐  ┌─────▼────┐  ┌──▼──────────┐    │
│    │ github_api  │  │   llm    │  │  exporter   │    │
│    │  (GitHub    │  │ (OpenAI  │  │  (Jinja2    │    │
│    │   REST API) │  │  兼容)   │  │  → HTML)    │    │
│    └──────┬──────┘  └────┬─────┘  └──────┬──────┘    │
│           │              │               │            │
│           └──────┬───────┘               │            │
│                  ▼                       │            │
│           ┌──────────┐                   │            │
│           │   db.py  │◄──────────────────┘            │
│           │ (SQLite3)│                                │
│           └──────────┘                                │
└──────────────────────────────────────────────────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │  templates/index.html│
         │  Vue3 + Tailwind CSS │
         │  + Fuse.js 模糊搜索   │
         │  + 暗黑模式           │
         └─────────────────────┘
```

---

## 三、模块详解

### 3.1 `main.py` — 主程序 & GUI 入口（524 行）

| 职责 | 说明 |
|------|------|
| **GUI 框架** | 基于 CustomTkinter 构建暗色主题桌面应用，窗口大小 920×680 |
| **三 Tab 布局** | ⚙️ 配置 → 🚀 任务 → 📤 导出 |
| **配置管理** | 读写 `config.json`，持久化 GitHub Token、LLM 参数 |
| **同步引擎** | 多线程并发（1-20 可调），通过 `ThreadPoolExecutor` 处理新项目 |
| **增量同步** | 基于 `owner_username` 维度对比远程与本地 ID 集合，只处理新增项 |
| **进度控制** | 实时进度条 + 成功/失败/跳过计数器 + 可随时停止 |
| **线程安全** | 所有 UI 更新通过 `self.after(0, ...)` 回到主线程 |

**同步主流程（`_sync_worker`）**：
1. 获取远程 Star 列表（分页，每页 100）
2. 与本地数据库比对，过滤已存在的项目
3. 对每个新项目，按优先级获取内容：Readme → Description → 文件树
4. 调用 LLM 分析（带 3 秒冷却保护）
5. 兜底：LLM 失败时回填 GitHub 原始数据

### 3.2 `github_api.py` — GitHub API 模块（193 行）

| 功能 | API 端点 | 说明 |
|------|----------|------|
| **获取 Star 列表** | `GET /user/starred` 或 `GET /users/{name}/starred` | 分页获取，支持获取自己或他人 |
| **获取 Readme** | `GET /repos/{name}/readme` | Base64 解码返回纯文本 |
| **获取文件树** | `GET /repos/{name}/contents/` | 顶层目录结构 |
| **获取仓库详情** | `GET /repos/{name}` | description、topics、language |
| **检测 Rate Limit** | `GET /rate_limit` | 剩余/总配额/重置时间 |

**关键设计**：
- 全局 `requests.Session` + 自动重试（5 次，指数退避，覆盖 429/5xx）
- `star+json` Accept Header 获取 `starred_at` 收藏时间
- 分页间 0.3 秒限速

### 3.3 `llm.py` — LLM 智能分析模块（168 行）

**核心策略 — 三级内容降级**：

```
优先级 1: Readme 文本（截取前 1500 字符）
   ↓ 失败
优先级 2: 项目 Description
   ↓ 失败
优先级 3: 文件目录结构（AI 推断）
   ↓ 全部失败
返回 None → 调用方回填 GitHub 原始数据
```

**LLM 调用特性**：
- 基于 `openai` SDK，兼容所有 OpenAI 格式 API（DeepSeek、Qwen、Kimi、Ollama 等）
- 最多重试 5 次，429 限流时等待 15~75 秒递增退避
- 可被用户停止信号随时中断（`_smart_sleep`）
- 输出格式：JSON（`summary` / `language` / `tags` / `category`）
- 分类体系：15 个预定义类别（AI与大模型、前端开发、后端开发等）

**System Prompt 要点**：
- 强制输出 300-350 字中文摘要
- 无论原文语言，必须输出中文
- tags 固定为 3 个技术标签
- category 从 15 个选项中严格选取

### 3.4 `db.py` — 数据库模块（138 行）

**数据库**：SQLite3，文件 `starmind.db`

**表结构 `starred_repos`**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | INTEGER PK | GitHub 仓库 ID |
| `name` | TEXT | 仓库全名（owner/repo） |
| `stars` | INTEGER | Star 数量 |
| `summary` | TEXT | AI 生成的中文摘要 |
| `category` | TEXT | 智能分类 |
| `tags` | TEXT | JSON 数组，技术标签 |
| `language` | TEXT | 主要编程语言 |
| `url` | TEXT | 仓库链接 |
| `description` | TEXT | GitHub 原始描述 |
| `processed_date` | TEXT | 处理时间戳 |
| `owner_username` | TEXT | Star 归属用户（增量同步键） |
| `starred_at` | TEXT | 收藏时间 |

**关键操作**：
- `upsert_repo()`：INSERT ... ON CONFLICT DO UPDATE（幂等写入）
- `get_existing_ids()`：按用户维度获取已存在 ID 集合
- 自动迁移：检测并添加 `owner_username` 和 `starred_at` 新字段

### 3.5 `exporter.py` — HTML 导出模块（49 行）

- 从数据库读取全部项目
- 收集所有分类和标签
- 使用 Jinja2 渲染 `templates/index.html`
- 输出到 `output/index.html`（约 1.8 MB 的自包含静态站点）

### 3.6 `templates/index.html` — 前端模板（255 行）

**技术栈**：Vue 3 (CDN) + Tailwind CSS (CDN) + Fuse.js (CDN)

**功能特性**：
- 🔍 Fuse.js 模糊搜索（项目名、摘要、标签、语言、分类）
- 📂 分类 pill 按钮筛选
- 🏷️ 标签点击过滤
- 📊 排序切换（Star 数量 / 收藏时间）
- 🌙 暗黑模式（跟随系统 + 手动切换）
- 🎨 毛玻璃导航栏 + 卡片悬浮动效
- 📱 响应式网格（1/2/3 列自适应）

---

## 四、配置与运行

### 4.1 依赖（`requirements.txt`）

```
customtkinter>=5.2.0    # GUI 框架
requests>=2.31.0        # HTTP 客户端
openai>=1.12.0          # LLM SDK
jinja2>=3.1.3           # 模板引擎
```

### 4.2 配置文件（`config.json`）

```json
{
  "github_token": "ghp_xxx",
  "github_username": "",
  "llm_base_url": "http://127.0.0.1:11434/v1",
  "llm_api_key": "xxx",
  "llm_model": "xxx"
}
```

### 4.3 打包发布

- `build.bat`：一键 PyInstaller 打包为 Windows exe（`--onedir --windowed`）
- 输出到 `dist/StarMind Manager/`
- 已生成分发包 `StarMind-Manager-Windows.zip`（约 47 MB）

---

## 五、代码统计

| 文件 | 行数 | 职责 |
|------|------|------|
| `main.py` | 524 | GUI + 同步引擎 |
| `github_api.py` | 193 | GitHub REST API 封装 |
| `llm.py` | 168 | LLM 调用与分析 |
| `db.py` | 138 | SQLite 持久化 |
| `exporter.py` | 49 | HTML 导出 |
| `templates/index.html` | 255 | 前端模板 |
| **合计** | **~1,327** | — |

---

## 六、版本演进（Git 提交记录）

| 提交 | 说明 |
|------|------|
| `bd5ca43` | **v1.0** — 初版：GUI 配置 / GitHub Star 同步 / LLM 分析 / HTML 导出 |
| `52e69c7` | **v1.1** — 增加进度恢复与按用户增量同步，放宽线程限制到 20 |
| `ee38f3f` | 深度优化 AI 中文分类与总结、新增网络自动重试、支持按收藏时间排序 |
| `f96054b` | 增加 3 秒延迟保护，适配免费 API 速率限制 |
| `b4d4290` | 修复 `starred_at` 字段丢失 + 429 限流智能退避 |
| `4425527` | 修复停止按钮因限流休眠导致的线程假死问题 |
| `4a95041` | 移除前端截断效果、放宽 `max_tokens` 确保完整输出 |
| `53cefea` | 增加 `build.bat` 一键打包脚本 |
| `6c07c3a` | 提供 Windows 一键运行安装包 |

---

## 七、亮点与设计考量

1. **多级内容获取降级**：Readme → Description → 文件树 → 原始数据，确保每个项目都有内容入库
2. **LLM 调用鲁棒性**：5 次重试 + 429 感知退避（15~75s）+ 可中断休眠
3. **多用户支持**：通过 `owner_username` 字段区分不同用户的 Star 数据
4. **增量同步**：只处理新增项目，避免重复调用 API 和 LLM
5. **全离线知识库**：导出的 HTML 是纯静态单文件，可本地打开或部署到任何静态托管
6. **LLM 兼容性**：支持所有 OpenAI 兼容 API（本地 Ollama、云端 DeepSeek 等）

---

## 八、潜在改进方向

| 方向 | 建议 |
|------|------|
| **安全性** | `config.json` 中明文存储 Token，建议加密或使用系统密钥链 |
| **搜索增强** | 导出 HTML 可增加高级搜索（按语言、Star 数量区间） |
| **批量更新** | 支持重新分析已入库项目（当前仅增量，无法刷新旧数据的 AI 摘要） |
| **多数据源** | 可扩展支持 GitLab、Gitee 等平台 |
| **数据统计** | 增加分类饼图、语言分布图等可视化分析 |
| **测试覆盖** | 当前无自动化测试，建议补充单元测试 |
| **国际化** | 当前 LLM Prompt 强制中文，可增加语言选项 |

---

*文档由 Claude 自动生成，基于对项目全部源码的完整阅读。*
